# GitWork

Reinventing the wheel, reinventing Git and making it Work

In order to add or remove files, there will be both a tree to navigate through and a search bar.
The destination of the local repository is chosen through the options above when first initializing.

- Removing "Selected" functionality given that we're planning to save the whole directory in which ".gitwork" is in.
